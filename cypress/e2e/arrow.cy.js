//Exemplo de 'it' utlizando Arrow Function
it('Exemplo de Cenário de Testes', () => {

})

//Função atribuída a variável
const soma = function (a, b) {
  return a + b;
}

//Arrow Function atribuída a variável
const somaArrow = (a, b) => {
  return a + b;
}

//Função comum
function soma(a, b) {
  return a + b;
}

//Arrow function de 1 linha - O return apenas é implicito quando utilizado da forma abaixo
const somaArrow2 = (a,b) =>  a + b;